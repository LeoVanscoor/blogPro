{% extends 'base.html.twig' %}

{% block title %}Modifier le commentaire{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Modifier le commentaire</h1>

        <div class="card p-4 shadow-sm">
            {{ include('commentaire/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
        </div>

        <div class="mt-3">
            <a href="{{ path('app_admin') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

    </div>
{% endblock %}